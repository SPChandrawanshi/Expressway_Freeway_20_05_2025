import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { ShieldAlert } from 'lucide-react';

const BlacklistedTags = () => <UniversalDashboard title='Tag Blacklist & Fraud Control' subtitle='Toll Operations Command Center' icon={ShieldAlert} />;
export default BlacklistedTags;