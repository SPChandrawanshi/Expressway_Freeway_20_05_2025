import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { List } from 'lucide-react';

const VehicleQueue = () => <UniversalDashboard title='Live Toll Lane Vehicle Queue' subtitle='Toll Operations Command Center' icon={List} />;
export default VehicleQueue;