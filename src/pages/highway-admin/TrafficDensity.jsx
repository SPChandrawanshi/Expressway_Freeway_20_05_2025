import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { Activity } from 'lucide-react';

const TrafficDensity = () => <UniversalDashboard title='Live Traffic Density & Flow Stats' subtitle='Highway Administration Command' icon={Activity} />;
export default TrafficDensity;