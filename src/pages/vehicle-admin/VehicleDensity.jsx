import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { Activity } from 'lucide-react';

const VehicleDensity = () => <UniversalDashboard title='Lane-wise Vehicle Density Stats' subtitle='National Vehicle Monitoring Command' icon={Activity} />;
export default VehicleDensity;