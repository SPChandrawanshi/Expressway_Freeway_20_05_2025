import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { Fuel } from 'lucide-react';

const PatrolTracking = () => <UniversalDashboard title='Patrol & Fuel Tracking' subtitle='Intelligence & Operations Command Center' icon={Fuel} />;
export default PatrolTracking;