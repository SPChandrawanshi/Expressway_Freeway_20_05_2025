import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { List } from 'lucide-react';

const EmergencyQueue = () => <UniversalDashboard title='SOS Priority Resolution Queue' subtitle='National Emergency Command Center' icon={List} />;
export default EmergencyQueue;