import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { Building2 } from 'lucide-react';

const TollBooths = () => <UniversalDashboard title='Plaza-level Toll Booth Operations' subtitle='Toll Operations Command Center' icon={Building2} />;
export default TollBooths;