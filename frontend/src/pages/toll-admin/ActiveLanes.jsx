import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { Zap } from 'lucide-react';

const ActiveLanes = () => <UniversalDashboard title='Live Lane Monitoring & Health' subtitle='Toll Operations Command Center' icon={Zap} />;
export default ActiveLanes;