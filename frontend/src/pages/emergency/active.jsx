import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { AlertCircle } from 'lucide-react';

const ActiveEmergencies = () => <UniversalDashboard title='Live SOS & Active Emergencies' subtitle='Super Admin Control Panel' icon={AlertCircle} />;
export default ActiveEmergencies;