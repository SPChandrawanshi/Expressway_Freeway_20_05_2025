import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { Map } from 'lucide-react';

const RoadBlockages = () => <UniversalDashboard title='Live Road Blockage & Diversion Tracking' subtitle='National Incident Command Center' icon={Map} />;
export default RoadBlockages;