import React from 'react';
import UniversalDashboard from '../common/UniversalDashboard';
import { Timer } from 'lucide-react';

const IncidentTimeline = () => <UniversalDashboard title='Step-by-step Incident Resolution Timeline' subtitle='National Incident Command Center' icon={Timer} />;
export default IncidentTimeline;